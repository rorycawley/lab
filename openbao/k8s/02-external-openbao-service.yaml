apiVersion: v1
kind: Service
metadata:
  name: external-openbao
  namespace: openbao-demo
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: http
      port: 8200
      targetPort: 8200
