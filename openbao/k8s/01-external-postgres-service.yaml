apiVersion: v1
kind: Service
metadata:
  name: external-postgres
  namespace: openbao-demo
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
