apiVersion: v1
kind: Service
metadata:
  name: external-pg-proxy
  namespace: keycloak-pg-demo
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: postgres
      port: 6432
      targetPort: 6432
