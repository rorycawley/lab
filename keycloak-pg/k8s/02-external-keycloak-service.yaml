apiVersion: v1
kind: Service
metadata:
  name: external-keycloak
  namespace: keycloak-pg-demo
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: http
      port: 8180
      targetPort: 8180
