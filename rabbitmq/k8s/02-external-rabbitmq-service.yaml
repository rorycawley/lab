apiVersion: v1
kind: Service
metadata:
  name: external-rabbitmq
  namespace: rabbitmq-demo
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
