apiVersion: v1
kind: Service
metadata:
  name: external-read-postgres
  namespace: registry-poc
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: postgres
      port: 5433
      targetPort: 5433
