apiVersion: v1
kind: Service
metadata:
  name: keycloak-oidc-demo-app
  namespace: keycloak-oidc-demo
spec:
  selector:
    app: keycloak-oidc-demo-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
