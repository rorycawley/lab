apiVersion: v1
kind: Service
metadata:
  name: external-event-postgres
  namespace: event-sourcing-demo
spec:
  type: ExternalName
  externalName: host.rancher-desktop.internal
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
